# Example values for Cloudian COSI Driver
s3:
  iamEndpoint: "https://iam.hyperstore.test:16443"
  s3Endpoint: "http://s3-region-1.hyperstore.test"
  region: "region-1"
  accessKey: "0a1b2c3d4e5f6g7h8i9j"
  secretAccessKey: "lkjhGFdsAPoiuYTreWQmnbVCxZlkjhGFdsAPoiuY+"
admin: # Optional: required only if using BucketClass.authenticationType "Key" (not recommended)
  endpoint: "https://s3-admin.hyperstore.test:19443"
  username: "sysadmin" # corresponds to hsctl admin.auth.username
  password: "password" # corresponds to hsctl admin.auth.password
  group: "group1" # the name of the HyperStore group to create users and buckets in
disableTLSCertificateCheck: false
existingS3CertificateSecret: ""
existingCredentialsSecret: ""
image:
  repository: quay.io/cloudian/cosi-driver
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""
sidecar:
  image:
    repository: gcr.io/k8s-staging-sig-storage/objectstorage-sidecar
    tag: v20241003-v0.1.0-58-g80979e8
